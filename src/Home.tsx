import { PureComponent } from "preact/compat";

export class Home extends PureComponent {
    render() {
        return (
            <div>
                <h1>Hello World</h1>
            </div>
        );
    }
}